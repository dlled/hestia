{
  "$schema": "https://turbo.build/schema.json",
  "globalPassThroughEnv": ["FORCE_COLOR", "DATABASE_URL", "LOG_LEVEL", "NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
